# 项目路线图 (Project Roadmap)

本文档旨在记录 `Epistasis Analysis Pipeline` 项目未来的开发计划与功能迭代方向。当前版本为 **v0.4**。

我们将基于当前稳定、高效的分析流程，继续探索和集成更多业界领先的实践，以进一步提升流程的准确性、易用性和稳健性。

---

### **v0.5: 更精细的变异质控 (Advanced Variant Recalibration)**

- **目标**: 引入 GATK VQSR (Variant Quality Score Recalibration) 作为高级选项，替代当前的硬过滤（Hard Filtering）策略。
- **动机**: 对于大规模样本（尤其是人类基因组），VQSR 使用机器学习算法对变异位点进行打分，相比硬过滤能更精确地区分真阳性与假阳性位点，在保证特异性的同时提升敏感性。
- **实施计划**:
    - [ ] 添加 GATK VQSR 相关规则/模块。
    - [ ] 在配置文件中添加入参，允许用户在“硬过滤”和“VQSR”模式间切换。
    - [ ] 更新文档，说明 VQSR 的使用场景和所需资源（如已知位点数据库）。

### **v0.6: 比对质量校准 (Base Quality Recalibration)**

- **目标**: 在变异检测前，增加 GATK BQSR (Base Quality Score Recalibration) 步骤。
- **动机**: BQSR 可以根据测序过程中产生的系统性错误来校正碱基的质量值，从而为下游的变异检测提供更准确的输入，进一步减少假阳性变异。
- **实施计划**:
    - [ ] 在比对（mapping）和标记重复（markdup）之后，加入 BQSR 相关步骤。
    - [ ] 在配置文件中提供开关，允许用户选择是否执行此步骤。

### **v0.7: Nextflow 流程全自动化 (End-to-End Automation for Nextflow)**

- **目标**: 将 `seq_preprocessor` 和 `json_md5_verifier` 工具的调用内嵌到 Nextflow 流程中。
- **动机**: 实现与 Snakemake 流程一致的用户体验，让 Nextflow 用户也能通过修改配置中的原始数据路径，实现“一键式”分析，无需再手动执行预处理步骤。
- **实施计划**:
    - [ ] 编译 Rust 工具的静态二进制文件，并将其放置在 `bin/` 目录中。
    - [ ] 编写一个新的 Nextflow `process`，用于调用上述二进制文件来处理原始数据。
    - [ ] 修改主流程 `Epistasis.nf`，将此过程作为分析的第一个环节。

### **v0.8: 自动化测试与持续集成 (CI/CD)**

- **目标**: 为项目建立自动化测试框架。
- **动机**: 确保代码在迭代更新过程中的稳定性和正确性，避免引入新的 bug。这是保障项目长期健康发展的关键。
- **实施计划**:
    - [ ] 准备一个微型测试数据集（mini test dataset）。
    - [ ] 编写测试脚本，该脚本能运行 Snakemake 和 Nextflow 流程，并验证其输出结果（如生成的 VCF 文件 MD5 值）是否与预期一致。
    - [ ] （可选）集成 GitHub Actions 等 CI/CD 工具，实现代码提交后自动触发测试。

### **v0.9: 文档增强 (Documentation Enhancement)**

- **目标**: 补充“方法论”与“参数选择”相关章节。
- **动机**: 让文档不仅告诉用户“如何做”，还解释“为什么这么做”，增加其科学内涵。
- **实施计划**:
    - [ ] 撰写一个新章节，详细解释流程中关键参数（如 `MAF >= 0.05`）的默认值及其选择依据。
    - [ ] 为不同应用场景（如不同物种、不同测序深度）提供参数调整的建议。
